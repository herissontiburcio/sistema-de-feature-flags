# Frontend fly config (deploy with --app <APP_NAME> or set secret FLY_APP_FRONTEND)

app = "sistema-de-feature-flags-frontend"

[build]
  dockerfile = "Dockerfile"

[[services]]
  internal_port = 80
  protocol = "tcp"

  [[services.ports]]
    handlers = ["http"]
    port = 80
